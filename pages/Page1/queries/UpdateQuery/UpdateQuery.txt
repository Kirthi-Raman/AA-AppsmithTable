UPDATE public."users" SET
    "gender" = '{{CreateUserForm.formData.gender}}',
    "email" = '{{CreateUserForm.formData.email}}',
    "name" = '{{CreateUserForm.formData.name}}'
  WHERE id = '{{Table1.selectedRow.id}}'; -- Specify a valid condition here. Removing the condition may update every row in the table!